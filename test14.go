/* package main

import "fmt"

func main() {				//	以下实例中将整型转化为浮点型，并计算结果，将结果赋值给浮点型变量：
	// var sum int = 17
	// var count int = 5
	// var mean float32 

	// mean = float32(sum) / float32(count)
	// fmt.Printf("mean 的值为: %f\n",mean)

	// go 不支持隐式转换类型，比如 :
	var a int64 = 3
	var b int32 
	// b = a
	//	但是如果改成 b = int32(a) 就不会报错了:
	b = int32(a)
	fmt.Printf("b 为 ： %d\n", b)
} */